# Notebooks

Notebooks are optional exploratory interfaces, not authoritative pipelines. Pin
inputs, metadata, calibrations, commit, environment, and random seeds; clear large
outputs and promote accepted analysis to tested package functions. No example
notebook is included until a permitted synthetic dataset is defined.

