# Heterodyne Detection

Multiplication of electrical content at `fE` by modulation at `fA` produces
components at `fA ± fE`. A dual-phase lock-in referenced to the coherent acoustic
source translates carrier-band content to baseband. Electronic nonlinear mixing
can create the same frequencies and therefore requires dummy-load controls.

