# Data

Raw data are immutable. Every processed result must reference raw data, metadata,
calibrations, and the acquisition commit. Synthetic, simulation, and measured
datasets remain explicitly separated. See the repository `data/README.md`.

