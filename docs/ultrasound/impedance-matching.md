# Impedance Matching

Each carrier configuration has its own measured complex load and verified network.
Record topology, values, tolerances, tuning state, frequency, forward/reflected
power, temperature, source impedance, and revision. Networks are `TBD`.

