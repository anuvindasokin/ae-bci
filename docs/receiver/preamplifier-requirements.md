# Preamplifier Requirements

The differential low-noise front end must pass the carrier and ±8 kHz sidebands.
Provisional useful analogue bandwidth is approximately 100 kHz to at least
1.5 MHz, marked `TBD`. Validate noise, CMRR, gain/phase, input range, loading,
stability, overload recovery, shielding, and every selected carrier.

