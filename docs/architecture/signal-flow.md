# Signal Flow

One coherent source supplies the acoustic drive and lock-in reference. A separate
isolated path drives the passive source dipole. Acquisition records raw X/Y,
settings, overload state, controls, calibration IDs, coordinates, and hashes before
offset correction, phase rotation, normalisation, analysis, and reporting.

