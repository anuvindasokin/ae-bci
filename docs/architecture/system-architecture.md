# System Architecture

```text
carrier → RF amplifier → power monitor → matching network → transducer
→ conductive medium + passive electrical dipole → differential receiver
→ carrier-band low-noise front end → coherent X/Y lock-in → data pipeline
```

Every carrier uses a separately calibrated transducer, matching network, amplifier
validation, acoustic field, thermal record, receiver response, and lock-in setup.

