# Pressure-Gradient Interaction

Spatial response may depend on pressure, its gradient, electric field, and
electrode sensitivity. Pressure and gradient maps must therefore be reported
separately from the candidate AE map. The applicable interaction model is `TBD`.

