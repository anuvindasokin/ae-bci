# Clock and Synchronisation

The lock-in reference must derive from the same coherent source as the selected
carrier. Record reference source, frequency, stability, sample clock, timestamps,
trigger path, latency, and drift. External clock support and accuracy are `TBD`.

