# Transducer Requirements

For each frequency, record manufacturer/model (`TBD`), aperture, focus, impedance,
matching, drive limit, hydrophone calibration, pressure map, focal dimensions,
insertion loss, heating, mounting, serial number, and revision. Never assume a
narrowband device covers multiple configurations.

