# Spatial Scanning Test

Scan a registered acoustic focus relative to a fixed known source and receiver.
Record commanded and measured coordinates, pressure, X/Y, controls, stage state,
and timestamps. Report raw samples, interpolation, grid, focal comparison,
localisation error, repeatability, and receiver-field dependence.

