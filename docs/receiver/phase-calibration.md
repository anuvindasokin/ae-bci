# Phase Calibration

Measure ultrasound-only offsets `X0`, `Y0` and rotation `θ` under the current
configuration. Recover signed voltage as `(X−X0)cos(θ)+(Y−Y0)sin(θ)`. Store phase,
frequency, time, temperature, cables, reference path, uncertainty, and drift.

