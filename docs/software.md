# Software

The Python 3.11+ package provides frequency-independent sideband calculations,
metadata validation, X/Y phase rotation, offset subtraction, response analysis,
linearity, spatial interpolation, SNR, artefact screening, integrity hashes, and
carrier comparison. Functions use SI units and do not silently modify inputs.

