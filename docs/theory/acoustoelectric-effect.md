# Acoustoelectric Effect

The working small-signal relation is `Δσ / σ = K × P`, where `σ` is local
conductivity, `Δσ` its pressure-induced change, `K` the interaction coefficient,
and `P` acoustic pressure. Values of `K`, linear range, and medium dependence are
`TBD` and require traceable measurement or literature support.

